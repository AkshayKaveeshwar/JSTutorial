const myStates = [
    "Karnataka",
    "Assam",
    "Kerala",
    1947,
    "Jammu And Kashmir",
    "Andhra Pradesh"
];

myStates.forEach((s) => (console.log(s)));
myStates.forEach((s) => console.log(s)); //same thing but clear brackets if needed.
